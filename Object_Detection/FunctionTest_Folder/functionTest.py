import numpy as np

# def functionCall(frameRate , classes):
	# frameRate.value = 10
	# classes = [1.1,2.2,3.3]
	# classes = np.array(classes)
	# #for i in range(3):
	# #	classes[i] = i


frameRate = 10
classes = []
#classes = [1.1,2.2,3.3]
#classes = np.array(classes)
for i in range(3):
	classes[i] = i

print(type(classes))
